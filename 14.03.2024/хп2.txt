BEGIN
    SELECT Чеки.id AS 'ID чека', Чеки.дата_время AS 'Дата и время', SUM(Чеки.сумма) AS 'Общая сумма', SUM(Позиции_чека.количество) AS 'Общее количество товаров'
    FROM Чеки
    JOIN Позиции_чека ON Чеки.id = Позиции_чека.id_чека
    GROUP BY Чеки.id;
END