BEGIN
    SELECT Изделия.название, Изделия.цена, Позиции_чека.количество, Позиции_чека.статус, Изделия.img
    FROM Чеки
    JOIN Позиции_чека ON Чеки.id = Позиции_чека.id_чека
    JOIN Изделия ON Позиции_чека.id_изделия = Изделия.id
    WHERE Чеки.id = check_id;
END